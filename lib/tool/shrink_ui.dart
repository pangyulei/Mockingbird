import 'package:flutter/cupertino.dart';

class ShrinkUI extends StatelessWidget {
  const ShrinkUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
